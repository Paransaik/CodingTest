def solution(begin, target, words):
    answer = 0

    def dfs(word, currnt):
        if currnt == 'target':
            return answer
        else:
            return 0

        for i in range(len(begin))
            hegin
            for

                answer += 1

    dfs(beign, '')